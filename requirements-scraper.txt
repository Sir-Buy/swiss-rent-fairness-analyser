# Originally pinned Playwright for headless homegate.ch scraping. Homegate is
# now behind DataDome CAPTCHA - pivoted to Flatfox's public JSON API, which
# only needs stdlib urllib (already in Python). All scraping deps are now in
# the main requirements.txt (pgeocode for canton derivation from PLZ).
#
# This file is intentionally kept empty so the README's references and the
# requirements-split pattern remain valid; install with:
#   pip install -r requirements-scraper.txt   # no-op
